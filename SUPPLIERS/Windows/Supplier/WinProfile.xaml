<Window x:Class="SUPPLIERS.WinProfile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SUPPLIERS"
        mc:Ignorable="d"
        Height="149.366" Width="340" Style="{StaticResource WinFont}" WindowStartupLocation="CenterScreen">
    <Border Padding="5">
        <StackPanel>
            <TextBlock x:Name="Title" Margin="5" HorizontalAlignment="Center" Text="Профиль" Style="{StaticResource Title}"/>
            <Separator Background="#BF5324"></Separator>
            <DockPanel Margin="5">
                <TextBlock Width="70" Style="{StaticResource label}">Название</TextBlock>
                <TextBox x:Name="NAME"></TextBox>
            </DockPanel>
            <Grid Margin="20,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Button Width="100" Grid.Column="0" Click="ButtonClose" Style="{StaticResource Button}">Отмена</Button>
                <Button Width="100" Grid.Column="2" Click="Save" Style="{StaticResource Button}">Сохранить</Button>
            </Grid>
        </StackPanel>
    </Border>
</Window>
